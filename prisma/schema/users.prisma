model Users{
    id String @id @default(uuid())
    first_name String
    middle_name String
    last_name String
    email String @unique
    password String
    created_ts DateTime @default(now())
    updated_ts DateTime @default(now())
    guides Guides[]
}